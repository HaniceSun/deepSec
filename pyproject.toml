[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deepSec"
version = "1.0.0"
description = "Deep Learning Prediction of Selenocysteine Sites"
readme = "README.md"
requires-python = ">=3.12"

authors = [
  {name = "Han Sun", email = "hansun@stanford.edu"}
]

[project.urls]
"Homepage" = "https://github.com/HaniceSun/deepSec"

[project.scripts]
deepSec = "deepSec.cli:main"

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

[tool.setuptools.packages]
find = { where = ["src"] }

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.package-data]
deepSec = ["data/*.txt*", "config/*.yaml"]
